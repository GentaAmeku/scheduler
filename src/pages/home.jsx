import React from 'react';
import { LayoutBox } from '../shared';
import { Calendar } from '../components';

export const Home = () => (
  <LayoutBox py={3}>
    <Calendar />
  </LayoutBox>
);

export default Home;
